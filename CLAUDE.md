# 项目工作规则

开始工作前按顺序阅读并遵守：

1. `AGENTS.md`：长期协作规则、隐私边界、测试与 PR 门禁。
2. `HANDOFF.md`：当前分支、活跃 PR、最近验证、已知问题与下一步。
3. `PROJECT_STATUS.md`：已验证里程碑和历史实验记录。

不可省略的基本规则：

- 只能修改当前 Git 仓库内的文件。
- 每次只完成一个明确的小步骤，未经允许不实现后续阶段。
- 审查/诊断默认只读；修改、推送和 PR 必须在用户授权范围内。
- 不确定且会改变研究结论、接口或隐私语义时，先说明并确认，不自行猜测。
- 完成任务后按 `AGENTS.md` 更新 `HANDOFF.md`；有验证过的新里程碑时同步更新
  `PROJECT_STATUS.md`。
- PR 在完成深度审查、针对性测试、完整测试和必要的结果对拍前保持 Draft。
