# 曲率收益的共同状态阶段交互

## 1. 问题

Issue #18 在完全相同的冻结状态、donor 和随机流下比较 `gamma=0/1`，发现
`gamma=1` 在三个 500 轮状态上的单步 proposal 相对收益都更高。Issue #24 的
20 配对种子无接受动力学却没有形成长期优势；Issue #27 又表明曲率核会降低查询
空间移动速度，并在按各自轨迹汇总的残差区间中呈现混合漂移。

这两类证据回答的问题不同。冻结探针估计固定状态上的算子差异，长期轨迹则同时
包含算子差异和由历史转移造成的状态分布差异。尤其是 Issue #27 的 baseline 与
candidate 已经处于不同合成表，不能把残差分箱的组间差直接解释为曲率权重关于
残差阶段的因果交互，也不能据此事后选择调度方向或阈值。

本阶段因此只回答：

> 在共同合成状态和共同随机条件下，`gamma=1` 相对 `gamma=0` 的 proposal 收益，
> 是否会从 marginal 初始态到标准闭环 500 轮态稳定增强或减弱？

本阶段是诊断，不实现状态调度，不接入默认生成器。

## 2. 估计量与配对单位

固定 seed `i` 和阶段 `s`，从同一个合成状态生成 200 对 proposal。每对共享
donor、参与掩码、独立初始复制 mask、Gibbs 坐标流和均匀随机流，唯一变量为曲率
权重。记第 `p` 对 proposal 的精确平方 workload 收益为 `G`，定义 seed-state
层面的曲率相对收益

\[
\Delta_{i,s}=\frac{1}{200}\sum_p
\left(G_{i,s,p}^{\gamma=1}-G_{i,s,p}^{\gamma=0}\right).
\]

主估计量是同一 seed 内的阶段交互

\[
I_i=\Delta_{i,500}-\Delta_{i,0}.
\]

`I_i>0` 表示曲率相对优势在 500 轮态更强，`I_i<0` 表示更弱。seed 是独立统计
单位；每个状态的 200 个 proposal 只降低条件蒙特卡洛误差，不能当作 4000 个
独立样本，也不进行 proposal 级显著性检验。

这个差分中的差分仍只覆盖两个预先固定的状态来源，不等于连续残差函数的导数，
也不证明任意中间状态上的单调调度。

## 3. 固定实验协议

- 数据配置：`test_300x10`，公开记录数 300、预定义 50-query 精确 target、公开
  schema 和 1-way marginal；
- 独立种子：固定 `30..39`，不复用既有正式曲率实验的 seed；
- 每个 seed 固定两个共同状态：
  1. 由该 seed 直接生成的 marginal 初始化态；
  2. 标准 0-sweep 残差驱动接受闭环运行 500 轮得到的 best 表；
- 500 轮状态只用于构造共同的冻结输入；两个曲率变体都不参与该状态的选择；
- 同一 seed 的两个状态共享标准闭环在初始阶段确定的首个非零方向 RMS，避免按
  后期状态重新标定温度；直接初始化表必须与闭环记录的初始表哈希一致；
- 每个状态 200 对 proposal，共 `10 × 2 × 200 = 4000` 对；探针不执行整代接受、
  重试、早停或 checkpoint 选择；
- baseline 为 `gamma=0`，candidate 为 `gamma=1`；
- 固定 `rho=0.01`、`eta=0.5`、proposal 内 `mu=0`、`tau=2`、8 sweep、最高
  三阶因子和条件 logit 护栏 30；
- donor 使用既有 geometric 抽样；初始态 `alpha=2`，500 轮态 `alpha=10`；
- Conda `gsd`、CUDA 0 串行运行；墙钟只作诊断，不是方法终点；
- 不读取真实训练表或测试表，不报告联合 TVD；当前是精确统计量下的无噪声原型，
  不是端到端 DP。

正式运行不得追加 seed、扫描 `gamma/rho/tau/sweep` 或根据结果改变阶段定义。

## 4. 主统计与预注册分类

对 10 个 `I_i` 报告均值、样本标准差、中位数、范围、正/零/负种子数，以及以
seed 为单位、自由度 9 的双侧 95% t 区间。预注册分类为：

1. 区间下界大于 0，且至少 8/10 个 seed 的 `I_i>0`：
   `curvature_advantage_strengthens_late`；
2. 区间上界小于 0，且至少 8/10 个 seed 的 `I_i<0`：
   `curvature_advantage_weakens_late`；
3. 其余：`state_interaction_inconclusive`。

同时分别报告初始态与 500 轮态的 10 个 `Delta`、95% t 区间和方向计数。阶段交互
的显著方向不能覆盖绝对效应：例如两个阶段都不利但晚期较少不利，只能说明交互，
不能据此推荐开启曲率。任何调度候选还必须结合两个阶段的绝对方向，并在后续独立
Issue 中预注册长期动力学终点。

## 5. 次要诊断

为解释交互而不改变主分类，按阶段和 seed 报告：

- `gamma=0/1` 的一阶收益、总二次项、净收益与正收益率；
- 查询步平方范数、改变行数、复制单元格数和 mask Hamming；
- 条件微步数、平均熵、最小/最大条件概率与最大绝对 logit；
- 冻结状态的 workload loss 和 RMS 计数残差；
- 状态构造、因子构造、Gibbs 抽样和完整探针墙钟。

这些量用于判断交互来自一阶项、曲率代价还是移动量变化，不在看到结果后升级为
新的成功门槛。

## 6. 语义与复现门禁

正式分类前必须全部满足：

1. 每个 seed 恰有一个直接初始态和一个 500 轮闭环状态，二者初始表哈希一致，
   方向参考尺度正有限；
2. 每对 proposal 的 donor、参与、完整初始 mask、主 RNG 与 Gibbs RNG 对齐；
3. `gamma=0` 与既有因子 Gibbs 的最终表、mask、共同诊断、RNG 端点和条件概率
   精确一致；
4. 内部查询变化与直接整表评价一致；行增量求和、
   `gain=linear-quadratic`、`quadratic=self+cross` 及 `N*V_1=gain` 的最大误差
   不超过 `1e-10`；
5. 条件 logit 护栏零命中，全部条件概率严格双向；所有正式数值有限；
6. 正式运行要求 tracked 工作树干净，输出拒绝覆盖并记录 commit、完整命令、环境、
   GPU、公开输入哈希和全部逐 proposal 原始结果；
7. 独立读取正式 JSON，重新计算 20 个 seed-state、10 个阶段交互、区间、分类、
   门禁与输入哈希；
8. `py_compile`、针对性测试、完整 CPU/torch/CUDA 测试和 `git diff --check`
   通过。

任一门禁失败时只报告诊断失败，不给出阶段交互分类。

## 7. 决策边界

本实验固定 `rho=0.01`，与 Issue #29 的参与率衰减调度正交。它不修改默认参数，
不实现残差阈值或内禀时钟控制，不接入 select-measure 流程，也不使用真实联合 TVD
驱动生成、阶段判断或输出选择。

若结果支持晚期增强或减弱，下一步仍需根据绝对阶段效应另立 Issue，冻结一个只依赖
公开查询残差或内禀时钟的有限温调度，并用无接受动力学检验。若结果不确定，则不
事后扫描阈值或参数，优先承认证据精度或两阶段概括不足。

关联 Issue #30、#29、#27、#24、#18。

## 8. 正式结果（2026-08-04）

### 8.1 主分类

正式实验基于干净提交 `1690738`，在 Conda `gsd`、CUDA 0、RTX 4090 上一次性
完成 seed 30..39 的 20 个冻结状态和 4000 对 proposal。没有追加、删除或替换
seed，也没有扫描曲率权重或状态阈值。

10 个 seed 的曲率净收益差如下：

| seed | 初始态 `Delta` | 500 轮态 `Delta` | 交互 `I` |
|---:|---:|---:|---:|
| 30 | +0.4100 | +1.0050 | +0.5950 |
| 31 | -2.0575 | +1.0125 | +3.0700 |
| 32 | -0.8400 | +0.9325 | +1.7725 |
| 33 | +0.2825 | +1.0000 | +0.7175 |
| 34 | -0.2175 | +0.9350 | +1.1525 |
| 35 | -0.5400 | +1.1275 | +1.6675 |
| 36 | -0.6725 | +1.2900 | +1.9625 |
| 37 | -0.4875 | +1.2250 | +1.7125 |
| 38 | -0.8475 | +0.9650 | +1.8125 |
| 39 | -1.0850 | +1.2100 | +2.2950 |

初始态 `Delta` 均值为 `-0.6055`，95% t 区间
`[-1.1082,-0.1028]`，方向计数为 2/0/8；500 轮态均值为 `+1.07025`，区间
`[0.9761,1.1644]`，10/0/0。主交互均值为 `+1.67575`，区间
`[1.15337,2.19813]`，10/0/0。因此同时通过区间下界大于 0 和至少 8/10 同向
两个预注册条件，正式分类为
`curvature_advantage_strengthens_late`。

这不仅是“晚期较少变差”：曲率在初始态的绝对相对收益为负，在 500 轮态的绝对
相对收益为正。晚期 proposal 正收益率也从 `7.45%` 提高到 `9.05%`，seed 级差
为 `+1.60` 个百分点，区间 `[+1.046,+2.154]` 个百分点，10/10 个 seed 改善；
初始态两侧正收益率都为 `92.25%`，没有形成方向差。

### 8.2 交互来源

| proposal 均值 | 初始 `gamma=0` | 初始 `gamma=1` | 初始差 | 晚期 `gamma=0` | 晚期 `gamma=1` | 晚期差 |
|---|---:|---:|---:|---:|---:|---:|
| 一阶收益 | 88.0525 | 86.2385 | -1.8140 | 3.6780 | 3.1700 | -0.5080 |
| 自身二次项 | 11.0583 | 10.4943 | -0.5640 | 8.2700 | 7.3253 | -0.9448 |
| 交叉二次项 | 1.7875 | 1.1430 | -0.6445 | 0.6480 | 0.0145 | -0.6335 |
| 总二次项 | 12.8458 | 11.6373 | -1.2085 | 8.9180 | 7.3398 | -1.5783 |
| 净收益 | 75.2068 | 74.6013 | -0.6055 | -5.2400 | -4.1698 | +1.0703 |

`net=linear-quadratic`。从初始态到晚期，candidate 相对牺牲的一阶收益缩小
`1.3060`，相对节省的二次项又增加 `0.36975`；两者合计正好得到主交互
`1.67575`。因此结果符合预期的尺度关系：残差大时一阶方向项占主导，完整曲率会
略微妨碍快速前进；残差缩小后，一阶收益空间下降，而有限整代查询步的二次过冲仍
然存在，曲率校正才转为净有利。

曲率核的查询步平方范数在初始态从 `25.6915` 降到 `23.2745`（-9.41%），晚期
从 `17.8360` 降到 `14.6795`（-17.70%）；复制单元格数分别从
`9.9095→9.4785`（-4.35%）和 `7.4910→6.8255`（-8.88%）。这说明晚期优势仍
伴随更强的查询空间缩步，不能把本结果误写成固定轮数长期收敛已经改善。

### 8.3 扩散支持、门禁与成本

初始态的平均条件熵为 `0.45192→0.46194`，晚期为
`0.68743→0.68410`。所有 1,054,528 个两侧条件微步都保持严格双向概率；全程
最大有效 `|logit|=10.7578`，低于护栏 30，截断次数为 0。晚期 candidate 的
条件概率范围为 `[0.07606,0.80067]`，没有退化为确定性门控。

20 个状态的直接初始化与闭环初始表哈希全部一致，方向参考尺度逐 seed 共享；
4000 对 donor、参与、完整初始 mask、主 RNG 和 Gibbs RNG 全部对齐。
`gamma=0` 与既有因子 Gibbs 的表、mask、诊断、RNG 端点和条件概率精确一致。
查询增量、行增量求和、二次分解与直接 gain 恒等式最大误差为 0，
`N*V_1=gain` 最大误差为 `1.14e-13`，稀疏线性一致性最大误差为
`2.78e-17`。

独立读取 37,003,052 字节的正式 JSON 后，重新计算了全部状态配对均值、8000 个
proposal 查询变化哈希与收益恒等式、20 个 seed-state、10 个阶段交互、t 区间、
方向计数、输入哈希和最终分类，均与脚本输出一致。实验总墙钟 461.81 秒；每 seed
的初始/晚期探针平均为 20.62/18.92 秒，500 轮状态构造平均 6.60 秒。探针额外运行
`gamma=0` reference 作精确回归，这些计时不是生产生成器性能比较。

正式输出为
`outputs/curvature_state_interaction/formal_10seed_2state_200p_tau2_sweep8.json`，
SHA-256 为
`cbdc8ce541e12f93931ead0fad78ffa1863f11cf97454675f64315c84aabe5ab`。
专项测试 46 项、完整 CPU/torch/CUDA 测试 605 项通过，脚本与测试通过
`py_compile` 和 `git diff --check`。

### 8.4 结论边界

共同状态证据支持的是“曲率相对收益随阶段增强”，并把 Issue #27 不同轨迹残差
分箱中的表观反转识别为不能直接用于选择调度方向的混杂证据。若继续曲率路线，合理
候选方向是低曲率起步、随公开查询残差下降而平滑增强，而不是早期强曲率、晚期衰减。

本实验只有两个状态端点，没有识别切换阈值、函数形状或长期最优权重。更重要的是，
晚期两侧平均原始 proposal 收益仍为负，且 Issue #24 已证明固定 `gamma=1` 的同轮数
长期轨迹不占优。因此不得在本 PR 接入调度或改变默认值；任何残差调度都要另立
Issue，预注册固定函数、`gamma=0/1` 对照、无接受长期动力学和按查询时钟解释。
本结果不涉及真实表评价、跨 workload、选择测量或 DP。
