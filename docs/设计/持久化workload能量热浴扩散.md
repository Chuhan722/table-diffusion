# 固定温单坐标平方 workload 热浴：生成质量未改善的负结果

## 0. 审查后的证据定位

本文件保留实验运行前冻结的协议、门槛和判定规则，但在 2026-08-06 的方法审查
后重新界定其证据含义。历史主指标是核直接定义和优化的平方能量，且对照是会从
1-way marginal 初始表逐渐退化到全域均匀分布的 `beta=0` 机制参考。因此末段平方
能量的 `-87.28%` 只能确认实现按预期施加了能量倾斜，不能作为方法有效性、合成
质量或相对现有生成器优势的证据。2026-08-11 第三轮审查后，为避免机器读取方
误解，主分类标签由 `supports_persistent_heatbath_smoke` 重命名为
`construction_energy_check_passed`（构造层能量检查通过），同族标签一并改为
构造检查语义；历史标签逐字保留在输出的 `legacy_classification` 字段，判定规则
与门槛不变。

本阶段真正有方法信息量的结果是负面的：在单一 `test_300x10`、单一 `tau=1`
配置上，candidate 相对同种子初始表虽然降低平方能量，却使同一 workload 的
normalized L1、未测量 3-way 和 4-way 误差分别恶化，其中 3-way 相对恶化
6.43%。这些离线指标是正式能量实验完成后追加的分析，不属于运行前预注册的主判断
标准，因此只构成当前配置下明确的探索性负面信号：它们不支持把“固定初始尺度、
固定温度、单坐标更新、返回末态”这一完整协议接入默认生成器，但不据此声称已经
严格否定该协议的生成有效性。现有实验也不能在平方能量形式、温度定标、平衡态采样
框架和单坐标更新粒度之间分配因果责任。本 PR 因而定位为“固定协议未见生成质量
改善”的探索性负结果报告，不声称已经验证有效生成方法，也不把负面信号单独归因于
L2/L1 目标错配。

仍然成立且可复用的证据只有实现层事实：增量查询与能量计算精确、16 状态 oracle
满足条件概率/细致平衡/平稳性、有限温度保留反向支持、完整轨迹可以独立重放。
当前没有与默认生成器的同预算对照，没有温度前沿或跨数据集证据，固定初始尺度在
状态漂移后的有效温度也未经验证；逐格精确枚举路径没有实际规模可行性证据。

## 1. 研究问题

现有最高三阶因子 Gibbs 与整代曲率 Gibbs 都在一代内的 donor-copy mask 上定义
有限温条件分布。每一代开始时，donor、参与集合和初始 mask 都会重新生成；真正跨
微步持续存在的合成表没有一个独立的状态转移核。因此，冻结 proposal 的条件分布、
有限 mask 混合、同步整代更新和长期表状态漂移会混在一起。

Issue #30 的共同状态实验进一步表明，固定曲率的相对收益会随生成阶段反转；但
Issue #24 和 #27 又没有支持固定曲率的同轮数长期优势。继续扫描曲率、参与率或
温度不能回答“临时 mask 是否是错误的扩散状态”这一结构问题。

本阶段研究一个不同层次的候选：以合成表本身作为持久状态，在每个随机扫描微步中
直接从 workload 能量的有限温条件分布重采样一个属性值。目标不是证明 heat-bath
或 Gibbs 这一标准原语本身具有新颖性。原预注册没有把实现语义与独立质量问题明确
分层；事后审查确认，前五项只能由定义、oracle 和实现测试验证，不能替代第六项的
独立质量评价：

1. 相对无向离散扩散具有可证明的期望正向倾斜；
2. 不通过 proposal 排序、拒绝、回滚或 checkpoint 选择得到改善；
3. 有全局表状态上的平稳分布和细致平衡语义；
4. 有限温度下保留真实的反向转移支持；
5. 每个微步立即更新表、查询答案和残差，不重置临时 mask；
6. 降低平方能量后，未被该能量直接定义的 L1 与未测量高阶质量是否改善。

该方向与 Diffusion Evolution 用随机扰动和目标引导共同变换总体分布的出发点一致，
但采用离散状态空间上的精确条件转移。离散或混合表格上的 energy-based sampling
也会使用 Gibbs/heat-bath，因此本阶段只把“持久表状态 + workload 能量 + 当前项目
隐私边界内的精确增量实现”视为待验证的项目方法，不提前作通用创新性声明。

## 2. 状态、参考扩散与能量

合成表记为

\[
S\in\mathcal S=\prod_{i=1}^N\prod_{g=1}^A\mathcal D_g,
\]

其中 `N` 是公开记录数，`D_g` 是公开 schema 给出的有限合法域。数值属性在当前
原型中按 schema 的闭区间整数域处理。预定义查询的计数答案为 `q(S)`，无噪声目标
为 `y`。本阶段固定等权平方 workload loss

\[
E(S)=\frac12\lVert y-q(S)\rVert_2^2.
\]

参考扩散 `K_0` 每个微步先从全部 `N*A` 个表坐标中均匀选择
`c=(i,g)`，再从 `D_g` 上均匀重采样该坐标。当前值也在候选域内，因此参考核有
自环。它只依赖公开记录数与 schema。

## 3. 有限温 workload 热浴核

固定除坐标 `c=(i,g)` 外的表状态。对每个 `v in D_g`，令
`S^(c<-v)` 表示把该坐标改为 `v` 的表。逆温强度为 `beta>=0` 时，条件分布为

\[
P_\beta(S_{ig}=v\mid S_{-(ig)})
=\frac{\exp\{-\beta E(S^{c\leftarrow v})/N\}}
{\sum_{u\in\mathcal D_g}\exp\{-\beta E(S^{c\leftarrow u})/N\}}.
\]

实现使用等价的相对收益形式

\[
\log w_v=\beta\frac{E(S)-E(S^{c\leftarrow v})}{N},
\]

再减去最大 log weight 做稳定归一化。共同常数不改变条件概率。

采样后立即把选中值、整数查询答案和当前 loss 更新到新状态。下一微步从新表随机
选择坐标并计算条件分布。loss 只定义转移概率和记录诊断，不执行 generation
acceptance、重试、回滚、早停或 best checkpoint。

### 3.1 参考退化与双向支持

- `beta=0` 时，所有 log weight 为零，条件分布精确退化为 `D_g` 上均匀重采样；
- 对有限 `beta`、有限 loss 和非空有限域，数学上的每个合法值概率严格为正；
- 当前值也有正概率，随机扫描核非周期；任意单元格都能以正概率改成任意合法值，
  整个有限表状态空间不可约；
- 正收益、零收益和负收益值都不设置资格门槛。有限温度允许 loss 暂时上升。

float64 稳定实现若出现指数下溢、非有限 log weight 或概率舍入为零，必须显式失败，
不能用静默概率截断继续声称精确 heat-bath 语义。正式协议要求这类事件为零。

### 3.2 平稳分布与细致平衡

定义

\[
\pi_\beta(S)=Z^{-1}\exp\{-\beta E(S)/N\}.
\]

固定坐标的条件重采样就是 `pi_beta` 的完整条件分布，因此该坐标核满足细致平衡。
坐标选择概率与状态无关，均匀随机扫描是这些可逆核的混合，仍满足

\[
\pi_\beta(S)K_\beta(S,S')
=\pi_\beta(S')K_\beta(S',S)
\]

并保持 `pi_beta`。这里的平稳分布是算法语义，不等于有限微步已经混合，也不保证
固定计算预算下优于现有生成器。

### 3.3 相对参考扩散的期望正向驱动

固定状态与坐标，记条件候选 loss 为随机变量 `E_v`。则

\[
m(\beta)=\mathbb E_{P_\beta}[E_v],\qquad
m'(\beta)=-\frac1N\operatorname{Var}_{P_\beta}(E_v)\le 0.
\]

所以增加有限 `beta` 不会提高同一状态、同一坐标下的条件期望下一步 loss；只要
不同合法值的 loss 不全相同，导数严格为负。这是相对 `beta=0` 参考扩散的方向
保证，不是逐样本单调下降保证。

同一条件分布还满足

\[
P_\beta
=\arg\min_P\left\{\frac1N\mathbb E_P[E_v]
+\frac1\beta\mathrm{KL}(P\Vert U_g)\right\},
\]

其中 `U_g` 是公开合法域上的均匀参考分布。因而正向驱动来自对完整有限温分布的
能量倾斜，而不是后置筛选。

### 3.4 平衡态采样器与优化器的边界

固定有限 `beta` 时，这个 Markov 链的目标是平衡分布 `pi_beta`，而不是极小点集合
`argmin E`。即使已经完全混合，从有限温平衡分布抽取的单个样本也会在低能区间
波动，不保证位于能量最小点。要把这类核当作优化器，通常还需要能被单独审查的退火
日程，或显式的 best-tracking/输出选择语义。本协议两者都没有：`beta` 由初始表
定标后固定，并一律返回第 3000 微步的当前表。

candidate 的四个 750 步窗口平均 loss 为
`1544.23/1274.07/1315.91/1342.66`：首期降低后回升并波动的形态与有限温低能区内
涨落相容，但本实验没有混合时间、自相关或稳态收敛诊断，因而不宣称有限轨迹已经
到达 `pi_beta`。它只说明“固定温平衡态采样器的末态”不能被默认当成“能量优化器
的收敛解”。

### 3.5 单坐标与块级更新的差异

本核的一次微步只改一行的一个属性块。workload 中的多属性合取会把多个坐标耦合；
如果一个有利的联合编辑必须经过暂时更高能量的中间表，单坐标链只能依靠有限温的
上坡通道穿过该能垒。主线的 donor-copy 更新则可在一个 proposal 中联合改动多个
相关字段，两者的邻域和混合性质不同。

这是可解释负结果的并行机制假设，不是本 PR 已验证的因果结论。当前没有同能量、
同温度强度和同候选评价预算的单坐标/块级对照，因而无法判断质量恶化中有多少来自
更新粒度。

## 4. 精确增量计算

改变一行的一个属性只会改变该行对查询 indicator 的贡献。对候选值 `v`，记

\[
d_v=a(x_i^{g\leftarrow v})-a(x_i),
\]

其中 `a(x)` 是该行对全部预定义查询的 0/1 indicator 向量。则

\[
q(S^{c\leftarrow v})=q(S)+d_v
\]

且在计数残差 `r=y-q(S)` 下

\[
E(S)-E(S^{c\leftarrow v})
=\langle r,d_v\rangle-\frac12\lVert d_v\rVert_2^2.
\]

实现必须由行级 indicator 构造全部候选 `d_v`，用上式得到候选 loss；采样后只
增量更新被选中候选的查询答案。小状态和实际配置测试都要用整表查询重算独立对拍。

## 5. 小状态完整枚举 oracle

固定一个可完整枚举的非加性小例子：

- `N=2`；两个类别属性 `a,b`，合法值均为 `0/1`；
- 三条查询：`a=1`、`b=1`、`a=1 and b=1`；
- 目标计数固定为 `[1,1,1]`；
- 枚举全部 `2^(2*2)=16` 张有序表；
- 检查 `beta=0, 0.7, 1.3`，不根据结果替换强度。

对每个 `beta` 构造完整 `16x16` 一微步转移矩阵，正式语义门禁为：

1. 每行和与 1 的误差不超过 `1e-12`，所有允许的单坐标转移与自环严格为正；
2. 模块条件概率与从完整 `pi_beta` 直接归一化得到的条件分布误差不超过
   `1e-12`；
3. `max |pi_i K_ij-pi_j K_ji| <= 1e-12`；
4. `max |pi K-pi| <= 1e-12`；
5. 图遍历确认不可约，所有状态自环为正；
6. 行级查询增量、增量查询答案、loss 收益恒等式与整表重算误差不超过
   `1e-12`；
7. 条件期望 loss 随三个预定 `beta` 单调不增；用条件方差独立复核解析导数。

枚举 oracle 验证定义与实现，不作为实际生成质量证据。

## 6. 实际配置多种子无接受冒烟（历史预注册）

### 6.1 固定协议

- 配置：`test_300x10`，公开记录数 300、公开 schema、预定义 50-query 精确
  target 和公开 1-way marginal；
- 初始化：既有 deterministic marginal 初始化；
- 配对种子：固定 `40..59` 共 20 个，不追加、删除或替换；
- 每条轨迹固定 3000 个单坐标微步，约等于一次随机扫描 sweep，全部跑满；
- baseline：`beta=0` 的公开合法域均匀参考扩散；
- candidate：固定无量纲强度 `tau=1`。对同 seed 初始状态，枚举全部坐标和合法值
  的非零 `gain/N`，以其 RMS 固定 `s0`，整条轨迹使用 `beta=tau/s0`，不逐步
  重新定标；若没有非零项则 `beta=0` 并记录失败门禁；
- 两侧共享同一初始表、每步坐标和每个合法值的 Gumbel 随机量。轨迹分叉后不强迫
  表、条件概率或选中值相同；两侧随机流端点必须一致；
- 唯一算法变量是 `beta=0` 与固定初始 RMS 定标后的 `tau=1`；
- 不使用 donor、参与率、复制 mask、独立变异、曲率、generation acceptance、
  重试、回滚、早停或 best checkpoint；
- 只返回第 3000 微步的当前表。best loss 只能作为离线诊断，不能选择输出；
- 运行时不提供真实训练/测试表路径，不报告联合 TVD；当前仍不是 DP。

初始 RMS 是固定尺度约定，不是强度扫描。`tau=1` 只回答候选是否能进入实际配置，
不作为最优温度或默认值推荐。

### 6.2 历史主终点与预注册分类

主终点是最后 750 个微步的平均当前 workload loss。通过全部语义门禁后：

- candidate 聚合主终点相对 baseline 至少降低 5%，且至少 14/20 个种子更低：
  `construction_energy_check_passed`（历史标签
  `supports_persistent_heatbath_smoke`）；
- 聚合降低但不足 5%，或只有 11--13 个种子改善：
  `construction_energy_check_inconclusive`（历史标签
  `persistent_heatbath_smoke_inconclusive`）；
- 聚合不降，或至多 10/20 个种子改善：
  `construction_energy_check_not_passed`（历史标签
  `persistent_heatbath_smoke_not_supported`）。

以上门槛和判定规则是运行前冻结的历史协议，不能在看到结果后修改。2026-08-11
第三轮审查指出原标签会被后续程序或研究者误解为方法有效，因此主分类名称改为
构造层能量检查语义；运行前冻结的历史标签逐字保留在输出的
`legacy_classification` 字段，重命名不改变任何判定门槛、数值或轨迹。式
`m'(beta)=-Var_beta(E)/N` 已经保证同状态、同坐标下的能量条件期望随 `beta`
不增；两条分叉有限时间轨迹的末段排序虽不是该式的逐步逻辑推论，这个终点仍与
核的构造目标循环，缺少独立的质量信息。后续引用该分类时必须同时写明
“构造层实现一致性检查”，不得把 `passed` 解释为方法有效性。

同时报告 final loss、全轨迹平均 loss、正/零/负实际步比例、正负步幅、改变值比例、
条件期望 gain、查询评价次数和墙钟。不以 60,000 个微步冒充独立样本；统计单位是
20 个 seed，种子级 95% 区间只作描述。

这项比较的 baseline 是候选自身的无向机制参考，不是当前完整生成器。它会破坏
已经利用 1-way marginal 的初始化并趋向全域均匀分布，因此即使通过，也只能检查
实现是否产生了预期的能量倾斜；不能据此声称动力学有效、合成质量改善或优于现有
donor-copy 扩散。

### 6.3 风险与语义门禁

预先记录两个限制表述的风险：

- candidate 全轨迹条件熵除以对应合法域最大熵的微步平均低于 50%：
  `conditional_concentration_risk`；
- candidate 对 loss 严格更差合法值的条件概率质量，全轨迹平均低于 1%：
  `uphill_support_mass_risk`。

严格正概率只证明形式支持；第二项用于区分“数学上非零”与仍有实际规模的反向
扩散。风险不事后替换主分类，但会阻止“保留了有意义探索”的表述。

正式分类前还必须满足：

1. 40 条轨迹全部跑满 3000 微步，初始表、初始 loss、尺度和随机流按 seed 对齐；
2. 每一步选中值的增量查询答案与记录的查询变化一致；每 100 个微步及最终状态用
   整表评价复核整数查询答案，结束后必须从保存轨迹摘要独立审计；
3. 每一步记录的 gain 等于前后 loss 差，最大误差不超过 `1e-12`；
4. candidate 在自身当前状态上的条件期望 loss 不高于同状态 `beta=0` 条件期望，
   最大违反量不超过 `1e-12`；
5. 所有正式概率严格为正、和为 1、数值有限，数值护栏或下溢命中次数为零；
6. 输出记录 commit、完整命令、环境、设备、公开输入哈希、全部种子和逐步 loss；
7. 正式运行要求脚本已经纳入当前 commit，工作树（含未跟踪文件）干净，输出拒绝
   覆盖；
8. 独立读取 JSON 复算每种子主/次指标、配对汇总、风险、分类与输入哈希。

任一语义门禁失败时，只能报告实现或实验失败，不给出算法分类。

## 7. 实现与测试边界

本阶段新增独立纯模块、枚举 oracle、研究脚本和测试，不接入 `run_evolution`，不
修改默认参数。测试至少覆盖：

1. 单行、两行以及含合取查询的手算增量；
2. 类别域和整数数值域的完整合法值；
3. `beta=0` 精确均匀退化、固定随机输入复现和 Gumbel categorical 抽样；
4. 16 状态完整转移矩阵门禁；
5. 当前值、自环、全部候选 loss 相同和 target 已匹配边界；
6. 空表、空属性域、答案/target 错位、负或非有限强度、非法表值和概率下溢；
7. 实际配置短程 CPU 冒烟；
8. `py_compile`、针对性测试、完整 CPU/torch 测试和 `git diff --check`。

当前实现是精确研究原型，不具备实际规模可行性证据。正式小表一次随机扫描只有
`300*10=3000` 个坐标微步；nltcs 为 `16181*16=258896` 个坐标，单次扫描在尚未
计算每步查询和合法值枚举成本前就多 86.3 倍，workload 也从 50 增至 1001 个查询。
初始尺度还会枚举全部坐标和合法值。因此查询因子预编译、批量增量或其它等价加速
是进入实际规模比较的独立前置条件，不能把当前 CPU 小表墙钟外推到 nltcs。

经本轮审查，代码留存形态确定为：保留独立研究模块、精确 oracle、复现脚本与相关
测试，不接入 `run_evolution`、不修改默认参数、不作为稳定公共 API。保留的理由是：
负结果的条件核、全转移矩阵与逐转移审计需要可复现，且后续温度/退火与更大块邻域
问题需要以它作为明确参考。若后续不再复用或维护成本超过证据价值，应另立纯工程
Issue 评估移出，不在本方法结论中删掉复现链。

正式输出保存在 `outputs/persistent_workload_heatbath/`，不提交 Git。原预注册计划
是在冒烟通过后比较当前扩散演化；审查后不再把历史冒烟视为有效性通过。若未来以
目标对齐能量重新主张方法有效性，必须另立 Issue，预注册与默认生成器在相同数据、
workload、初始化及同查询评价预算/同墙钟下的直接比较。

关联 Issue #32、#30、#27、#24、#18。

## 8. 结果

### 8.1 首次正式运行的数值门禁中止

提交 `c2eafd1` 上首次执行
`PYTHONPATH=src conda run --no-capture-output -n gsd python scripts/probe_persistent_heatbath.py`
时，在 seed 40 尚未完成前触发“有限温条件期望 loss 高于 beta=0 参考扩散”门禁，
未写出 JSON，也不给出算法分类。定位
表明失败来自参考侧的浮点消减：83 个合法值、候选 loss 约为 `1.2e4` 时，直接计算
`dot(p, loss) - mean(loss)` 得到 `5.46e-12` 的假正差；先减共同最小 loss 后，
同一差值为 `7.11e-15`。

为定位问题，曾临时放宽模块内部容差并完成 seed 40 轨迹，观察到主终点
`11107.1987 -> 1488.9687`。这是失败后看到的单 seed 调试结果，不进入正式判断，
也没有据此改变强度、种子、步数或门槛。修复只将参考优势改为平移不变的 offset
计算；条件概率、Gumbel 输入和状态更新均未改变。新增 83 值域回归后，seed 40 的
3000 步原配置最大数值违反量为 `4.26e-14`，其余语义门禁全部通过。

该次失败本身没有算法分类；随后只在包含数值修复的干净提交上按同一协议从头运行，
结果见下一节。

### 8.2 历史预注册能量检查（非有效性证据）

数值修复后的正式生成基于干净提交 `1471051`，完整执行
`PYTHONPATH=src conda run --no-capture-output -n gsd python scripts/probe_persistent_heatbath.py`。
20 个 seed 的 40 条轨迹均跑满 3000 微步。主终点从无向参考扩散的
`10555.7663` 降到 candidate 的 `1342.6576`，相对降低 87.28%；配对差均值为
`-9213.1087`，95% t 区间 `[-9658.0813,-8768.1360]`，20/0/0。通过预登记的
5% 与 14/20 门槛，原始输出分类为 `supports_persistent_heatbath_smoke`。该标签
现在只作为历史协议结果保留，不是方法支持结论。

这个巨大相对差主要受 baseline 退化支配。`beta=0` 会从已经利用 1-way marginal 的
初始表持续向全域均匀参考分布扩散：四个 750 步窗口的平均 loss 依次为
`2882.97/5049.51/7670.23/10555.77`。candidate 的对应窗口为
`1544.23/1274.07/1315.91/1342.66`。作为事后尺度检查，candidate 最终当前 loss
相对同 seed 初始表从 `2155.35` 降到 `1371.80`，平均差 `-783.55`，区间
`[-880.65,-686.45]`，同样 20/20 改善；但第二窗口以后略有回升，说明固定有限温
核在接近平稳区间后仍保留真实波动，不是逐步下降或 best 选择。

方向来自分布而非接受：candidate 每 3000 步平均有 956.6 个降 loss、1108.4 个
零变化和 935.0 个升 loss 微步，没有任何一步被拒绝或回滚。条件期望的当前状态
gain 均值从参考侧 `-3.3370` 变为 `+0.2493`；相对同状态均匀条件分布的期望 gain
为 `+3.2075`。candidate 平均保留 91.62% 最大条件熵，严格 uphill 合法值的概率
质量为 31.20%，两个预登记探索风险均未触发。全部正式概率严格为正，观测最小值
为 `7.04e-4`；条件期望最大数值违反量仅 `4.26e-14`。

16 状态 oracle 的条件概率最大误差为 `1.11e-16`，查询增量与 loss/gain 恒等式
误差为 0；最大细致平衡误差 `1.73e-18`、平稳性误差 `1.39e-17`，三个 beta 均
不可约且有自环。保存前审计和全新进程只读审计都复算了 20 个初始状态、20 条随机
日程、40 张最终表和 120000 次转移，零失败。

CPU 上每 seed 的初始尺度枚举平均 3.086 秒，配对生成平均 12.544 秒；baseline 与
candidate 的 3000 微步内核分别为 4.507/4.472 秒。正式生成阶段耗时 250.884 秒，
不把保存前重复审计时间混入这一数字。正式 JSON 大小 51,347,863 字节，路径为
`outputs/persistent_workload_heatbath/formal_20seed_3000step_tau1.json`，SHA-256
为 `27da4e8fdf94bd060d49157b2d8b20b54727da7cf94fcfb8605f607f419dd39c`。

### 8.3 核心负面观察：生成后离线质量恶化（探索性，非预注册主判断）

正式分类完成后，提交 `6ee4034` 上单独运行
`PYTHONPATH=src conda run --no-capture-output -n gsd python scripts/analyze_persistent_heatbath_offline.py`。
离线脚本先重建并验证 20 张初始表与 40 张最终表，之后才读取单一真实参考表；这些
指标没有进入生成、参数、早停、分类或输出选择。提交 `f5f425a` 的第一次启动因脚本
同目录导入方式错误，在读取正式 JSON 和参考表前退出，没有输出；`6ee4034` 只修正
脚本执行入口。

离线结果构成本阶段的核心方法观察。这组指标是正式能量实验完成后追加的离线
分析，不属于运行前预注册的主判断标准，因此按探索性负面信号报告：

- candidate 当前表平方 workload loss 相对初始表 `2155.35 -> 1371.80`，20/20
  改善；但同一 50-query workload 的 normalized L1 从
  `0.017713 -> 0.019767`，只有 3/20 改善；
- 未测量 3-way mean L1 从 `0.0101981 -> 0.0108538`，相对恶化 6.43%，仅
  1/20 改善，触发 5% 完整质量风险；未测量 4-way 从
  `0.0049748 -> 0.0051552`，相对恶化 3.63%，3/20 改善；
- 分箱联合 TVD 从 `0.98583 -> 0.98983`，区间差异很小且方向为 7/1/12；原始联合
  TVD 在三种状态下都约为 1，已接近饱和，不能提供细粒度结论；
- 原始唯一状态数 `300.00 -> 299.75`，只下降 0.083%，没有支持集收缩风险。

candidate 相对无向 baseline 的 workload L1、未测量 3/4-way 和分箱 TVD 均明显
更好，但无向 baseline 本身会破坏强初始化，不能用这组相对优势覆盖 candidate 相对
初始表的 L1 与 3-way 退化。该数据集没有独立 train/test 划分，联合指标只属于单一
参考表的生成后诊断。

离线 JSON 大小 92,619 字节，路径为
`outputs/persistent_workload_heatbath/offline_formal_20seed_3000step_tau1.json`，
SHA-256 为 `375afa01d30102140d22749e837f0e200587c771ae86f477a6363f40e3a80e35`；
全新进程复算 20 个 seed、60 张表、正式结果与参考表哈希后通过。

### 8.4 结论与下一问题

本阶段的核心方法观察是明显负面的，但属于正式能量实验后追加的探索性分析，不是
预注册主判断：在唯一测试的数据集和温度上，平方 workload 能量下降没有转化为同一
workload 的 normalized L1 或未测量高阶质量改善，并在多数种子上恶化。因此当前
“平方能量 + 初始 RMS 固定温度 + 单坐标平衡态采样 + 末态输出”协议没有证据支持
作为有效生成方法接入默认 pipeline；本 PR 不提供相对默认生成器的优势证据，也不
声称这些探索性指标已经严格否定该协议的生成有效性。

可保留的正面事实限于实现语义：持久表状态 heat-bath 与理论条件分布一致，增量
计算和重放审计精确，有限温度没有退化为接受/拒绝筛选并保留实际反向概率。这些
事实只说明研究基础设施可复用，不说明合成方法有效。

现有证据不能区分四个并行解释：平方能量与 L1/未测量高阶评价的不一致；`beta` 只按
初始 RMS 定标而在状态漂移后失配；固定有限温平衡态采样器与 `argmin` 优化目标
不同；单坐标邻域在强相关查询上存在能垒或混合限制。本 PR 既不单独证实，也不排除
其中任一项。

结论覆盖面必须保持窄：只测了 `test_300x10` 和 `tau=1`，没有温度前沿、跨数据集、
默认生成器同预算对照或实际规模运行。在观察 seed 40..59 后直接追加自适应 `beta`
或多温度对照，会改变 Issue #32 事先冻结的唯一变量并产生事后选择，因而不在本 PR
补做。温度/退火、局部最小逃逸和更大块邻域应使用新种子和独立预注册；其中漂移约束
退火的证明义务与阶段门禁已由 Issue #40 独立记录。本阶段仍是精确 target 的无噪声
原型，不是 DP。
