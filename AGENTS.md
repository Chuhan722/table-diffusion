# 项目代理协作规范

## 1. 作用范围与文件分工

本文件适用于整个仓库，记录长期稳定的协作规则。所有自动化代理和人工协作者在
修改代码、运行实验或提交 PR 前都应遵守。

- `AGENTS.md`：稳定规则、质量门禁、隐私边界和工作流程。
- `HANDOFF.md`：当前分支、活跃 PR、最近验证、阻塞项和下一步，随交接更新。
- `PROJECT_STATUS.md`：已经完成且有证据支持的里程碑与历史实验记录。
- `docs/设计/`：具体算法设计、公式、边界和决策依据。
- `CLAUDE.md`：面向会自动读取该文件的工具的入口，规则应与本文件一致。

不要把短期状态写进本文件；不要把长期规则只写在 `HANDOFF.md`。

## 2. 开始工作前的固定顺序

1. 确认当前目录是本仓库，并执行 `git status --short --branch`。
2. 完整阅读 `AGENTS.md`。
3. 完整阅读 `HANDOFF.md`，确认当前分支、活跃 PR 和未完成事项。
4. 阅读 `PROJECT_STATUS.md` 的当前阶段和与任务相关的最近变更。
5. 阅读任务涉及的设计文档、实现和测试；不要只依据交接摘要修改代码。
6. 若任务涉及 GitHub，查询 PR 的实时状态，不把文档中的状态当成实时事实。

信息不一致时按以下顺序核验：当前代码与测试、当前 Git/GitHub 状态、实验原始输出、
`HANDOFF.md`、`PROJECT_STATUS.md`、设计文档。发现冲突要在本次任务中明确记录并
修正文档，不能默默选择一个版本继续。

## 3. 任务边界

- 每次只完成一个明确的小目标。性能、算法方法、DP、实验基础设施和纯文档尽量
  分成独立分支与 PR。
- 未经用户授权，不实现“下一阶段”或顺手改变默认算法参数。
- 审查或诊断任务默认只读；只有用户要求修改或提交时才写文件、推送或创建 PR。
- 不确定的设计选择如果会改变研究结论、公开接口或隐私语义，必须先说明选项和
  影响；不能自行猜测。
- 保留用户已有的未提交修改。不得用 reset、checkout 覆盖或其他破坏性方式清理。

## 4. 隐私边界

生成器运行时只允许使用：

- 公开 schema；
- 公开记录数；
- 预先定义的查询；
- 已发布或已经过 DP 测量的统计量；
- 显式提供的随机种子和算法参数。

训练集和测试集只能用于生成结束后的离线评价，不能进入初始化、每轮接受检查、
调度、早停或 checkpoint 选择。完整联合 TVD 也只能离线报告，不能偷偷成为运行时
目标。

当前仓库仍是无噪声原型。只有同时实现并验证以下要素后才能称为 DP：

- 明确的邻接关系和威胁模型；
- 噪声机制及其敏感度；
- ε/δ 与 accountant；
- 数据依赖查询选择所消耗的预算；
- 带噪边缘的一致性处理；
- 端到端隐私说明和测试。

“后处理不额外消耗隐私预算”不等于上游精确计数、查询选择或测量机制已经满足 DP。

## 5. 实验纪律

每个正式实验在运行前写清：假设、唯一变量、baseline、candidate、不变量、种子、
轮数或时间预算、设备和评价指标。

- 算法比较优先使用配对种子；多种子串行或并行方式要记录。
- 同轮数和同墙钟是不同问题，不能混为一个结论。
- 三种子结果只能作为较强方向性证据；显著性结论应结合样本量、效应大小和多重
  比较风险谨慎表述。
- 性能优化必须同时报告结果等价性、墙钟、关键阶段次数和内存；不能只报告总时长。
- 方法优化至少同时报告训练 workload、未测量高阶查询、训练/测试 TVD、支持集
  指标和运行成本，避免只挑一个有利指标。
- 真实训练/测试数据的评价必须和生成调用隔离，并在脚本或文档中明确标注。
- 记录 Git commit、完整命令、环境、GPU、输出目录和关键诊断。`outputs/` 默认不
  提交 Git，只在 `PROJECT_STATUS.md` 或 `HANDOFF.md` 记录可复现实验位置。

## 6. 修改与测试阶梯

修改前先阅读相关实现和既有测试。修改后按风险逐层验证：

1. 语法检查和 `git diff --check`。
2. 与修改直接相关的针对性测试。
3. 完整 CPU/torch 测试集。
4. 性能或重构 PR：旧版/新版同种子轨迹、最终表和质量指标对拍。
5. 算法 PR：小规模冒烟后再做多种子正式实验，并保留失败结果。
6. DP PR：除功能测试外，增加预算核算、邻接数据和隐私边界测试。

当前机器可先检查 `conda env list`，常用命令为：

```bash
PYTHONPATH=src conda run -n qdte python -m pytest tests/test_evolution.py -q
CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src conda run -n gsd python -m pytest -q
CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src conda run -n de python scripts/run.py
```

环境能力可能变化：`qdte` 适合 NumPy/Scipy 测试，`gsd` 当前包含 pytest 与 torch，
`de` 当前适合正式 CUDA 运行但可能没有 pytest。运行前实际验证，不要只信文档。

## 7. 合入前深度审查门禁

创建正式 PR 前必须完成本地深度审查。若为了协作必须提前开 PR，应先设为 Draft，
通过门禁后再改为 Ready。

- 分支基于最新上游 `master`，工作树状态清楚，PR 只包含一个主题。
- 逐行审查 diff，检查缓存/状态生命周期、随机数消耗、CPU/CUDA 分支、重试、提前
  停止、零轮和异常输入。
- 针对性测试与完整测试通过；不能只依赖一次成功实验。
- 性能/重构改动必须证明算法轨迹和最终结果等价，允许且只允许明确记录的非决策
  浮点诊断微差。
- 算法改动必须列出 baseline 的准确含义，并区分训练目标改善和离线泛化指标。
- 文档、测试数、实验数字、分支和 PR 状态已同步。
- `HANDOFF.md` 已更新，下一位协作者无需依赖聊天记录即可继续。

审查发现不属于当前 PR 的问题，不要顺手混入；记录到 `HANDOFF.md` 的已知问题和
下一步，并另开任务。

## 8. Git 与 PR 约定

- 从最新 `origin/master` 创建语义清楚的分支，如 `perf/...`、`feat/...`、
  `fix/...`、`docs/...`。
- 提交信息、PR 标题、PR 正文和面向项目的差异说明使用中文；代码标识符保持项目
  现有英文风格。
- 一个提交应可解释、可测试；一个 PR 只解决一个主题。
- PR 正文至少包括：动机、改动、未改内容、测试、实验/等价证据、风险与适用边界。
- 没有用户明确授权不得合并 PR、改写上游历史或删除远程分支。
- GitHub CLI 当前位于 `.conda/gh-cli/bin/gh`；使用前先确认登录账号和目标仓库。

## 9. 文档与交接更新

任务完成时：

- 有验证过的新里程碑或实验结论：更新 `PROJECT_STATUS.md`。
- 当前分支、PR、测试、阻塞项或下一步变化：更新 `HANDOFF.md`。
- 长期流程或质量门禁变化：更新 `AGENTS.md`，并检查 `CLAUDE.md` 是否仍一致。
- 只记录实际完成和验证过的事实；预算不足、时间结束或“计划要做”不能写成完成。

交接前确认工作树是否干净。若不干净，必须在 `HANDOFF.md` 列出每个未提交文件、
原因和下一步，不允许只写“还有一些修改”。

## 10. 仓库地图

- `src/table_diffevo/`：核心生成、评价、距离、抽样、更新和初始化实现。
- `tests/`：单元、回归、CPU/CUDA 一致性测试。
- `scripts/`：运行、扫描、对照和离线分析脚本。
- `configs/`：schema、查询工作负载和初始化配置。
- `data/`：基准数据；生成运行时不得越过隐私边界读取原始表。
- `docs/设计/`：设计方案和实验解释。
- `outputs/`：本地实验输出，默认被 Git 忽略。

## 11. 明确禁止

- 不得把“尚未实现 DP”描述成“已经 DP”。
- 不得用真实联合 TVD 驱动生成、早停或选择最优表。
- 不得为了漂亮结果删除失败种子或更换 baseline 而不披露。
- 不得只凭三种子 p 值宣称通用最优。
- 不得把原始数据、凭证、授权码、大型输出或环境目录提交到仓库。
- 不得在没有等价性证据时把性能重构描述为“结果不变”。
- 不得在未完成深度审查与测试时把 Draft PR 标记为可合入。
